{{>header}}
{{>navbar}}
<section class="container">
    <h3 class="text-center text-primary mt-5">Add User</h3>
 <table class="table table-stripped">
            {{#if show}}
            <thead>
            <th>ID</th>
            <th>firstName</th>
            <th>lastName</th>
            <th>email</th>
            <th>Actions</th>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.first_name}}</td>
                <td>{{this.last_name}}</td>
                <td>{{this.email}}</td>

                <td>
                    <a href="/user/{{id}}" class="btn btn-primary mx-3">Show</a>
                    <a href="#" class="btn btn-danger mx-3">Delete</a>
                    <a href="#" class="btn btn-success mx-3">Edit</a>
                </td>
            </tr>
            {{/each}}
            {{else}}
             {{#if resData}}
            <thead>
            <th>userId</th>
            <th>id</th>
            <th>title</th>
            <th>actions</th>
        </thead>
        <tbody>
            {{#each resData}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.userId}}</td>
                <td>{{this.title}}</td>
                <td>
                    <a href="/albums/{{id}}" class="btn btn-primary mx-3">Show</a>
                    <a href="#" class="btn btn-danger mx-3">Delete</a>
                    <a href="#" class="btn btn-success mx-3">Edit</a>
                </td>
            </tr>
            {{/each}}
            {{else}}
            <tr class="alert alert-danger"> 
             <td colspan="4" >
                 ni users here
             </td>
            </tr>
            {{/if}}
            {{/if}}
              
        </tbody>
    </table>